# Atoms Demo 交付说明

## 交付结论

项目已交付一个可在线运行、可公开检查源码的 Atoms-like AI App Builder：

- 在线地址：<https://atoms-demo.beanbunwang-lab.workers.dev>
- GitHub：<https://github.com/BeanBunWang/atoms-demo-2>

这不是只复刻创作页外观的静态样例。用户可以不选择模式，直接输入需求；系统完成意图识别、必要澄清、动态计划、人工审批、受控工具执行、产物校验与反馈。计算器和贪吃蛇已具备领域交互运行时：计算器可以点击完成四则运算、百分比和正负号；贪吃蛇可以开始、暂停、重置、方向控制、碰撞判定和计分。同一会话继续提出修改时，系统会基于已有产物生成新的修改计划，不复用上一轮过期意图或计划。

## 实现方案

核心闭环是：

`自然语言需求 → 意图识别 → 必要时澄清 → 动态计划 → 用户批准 → Agent/工具执行 → 结构化产物 → 校验/修复 → 可操作 App Viewer`

| 层 | 实现 | 代码位置 |
| --- | --- | --- |
| 意图与路由 | 区分构建、修改、研究、数据、增长、视频，并识别 calculator / snake 领域 | `agent/intent.mjs` |
| Agent loop | plan 与 build 两阶段运行；审批前停止；按依赖调度专家和工具；流式返回状态 | `agent/runtime.mjs` |
| 模型协议 | DeepSeek Flash 输出意图、计划和应用 schema；修改任务明确以已有 preview 为基线 | `agent/prompts.mjs`、`agent/model.mjs` |
| Tool use | 研究、产品、设计、应用组合、数据、增长、分镜和验证均为受控本地函数 | `agent/tools.mjs` |
| 交互产物 | `appType` 将通用 UI schema 路由到计算器、贪吃蛇或通用交互；计算器与游戏使用确定性 reducer | `site/js/interactive.js`、`site/js/app.js` |
| 增量修改 | 每轮提交清空旧 intent / runtimePlan / runtime events；只在 build 阶段发送本轮已批准计划，并保留已有 appType 与主题 | `site/js/planner.js`、`site/js/app.js` |
| Viewer 与状态 | Preview、Code、Design、Library、Theme、设备尺寸、Console/Files 和交互状态统一保存 | `site/js/viewer.js`、`site/js/storage.js` |
| 部署边界 | Node 服务负责本地运行；Cloudflare Worker 承担静态托管、同源 API、模型代理和限流 | `server.mjs`、`worker.mjs`、`wrangler.jsonc` |

完整预览不再导出一份容易漂移的静态 HTML，而是以 `?preview=<workspaceId>` 打开同源只读工作台视图，复用主 Viewer 的交互运行时。因此主 Viewer、完整预览和增量修改使用同一份 workspace schema。

## 技术取舍

| 选择 | 原因 | 放弃或延后的方案 |
| --- | --- | --- |
| 受控 schema + 领域 reducer | 交互可测试、可持久化，也不会执行模型注入的任意代码 | 浏览器直接执行模型生成代码；能力更自由，但需要真正的隔离沙箱 |
| 语义交给模型，执行边界确定化 | 既保留自然语言理解，又能稳定验证工具输入和产物 | 让模型直接输出命令或完整 runtime；失败面和安全风险过大 |
| plan/build 分阶段并保留审批门 | 计划可见、可改，执行边界清楚 | 收到输入立即执行；速度更快但不符合 Atoms 的可控创作体验 |
| 增量修改保存产物、不保存过期计划 | 保留应用身份、主题和内容，同时避免二次修改误用旧上下文 | 每次全量重建；实现简单但容易丢失用户已有结果 |
| `localStorage` 本地工作区 | 无需账号即可体验，刷新可恢复，适合作业交付 | 账号、数据库和多人协作；超出本轮核心链路范围 |
| Cloudflare Worker 托管与代理 | 免费层可托管静态资源和保存服务端 Secret，部署链路短 | 独立前后端与数据库；当前收益不足以抵消运维复杂度 |

## 当前完成程度

| 能力 | 状态 | 可验证结果 |
| --- | --- | --- |
| 无模式快速开始 | 已完成 | 直接输入需求，自动识别意图与领域 |
| 澄清、动态计划、审批 | 已完成 | 仅在方向会明显分叉时澄清；审批前不执行工具 |
| Agent runtime 与 Tool use | 已完成 | 流式展示 Agent、tool call/output、依赖、验证和完成状态 |
| 计算器生成 | 已完成 | 四则运算、清空、退格、小数、百分比、正负号和历史记录可操作 |
| 贪吃蛇生成 | 已完成 | 开始、暂停、重置、方向控制、移动、食物、碰撞、得分和最高分可操作 |
| 增量修改 | 已完成 | 新一轮识别为 `modify_app`，生成独立计划并保留原应用类型/主题 |
| App Viewer | 已完成 | Preview/Code、Design/Library/Theme、三档设备、Console/Files、完整预览 |
| 工作区状态 | 已完成 | 多项目、刷新恢复、附件、JSON 导出与交互状态持久化 |
| 在线真实模型 | 已完成 | Cloudflare Worker 调用 `deepseek-v4-flash`；Key 仅在服务端 Secret |
| 通用业务应用专属逻辑 | 部分完成 | 通用应用支持导航、CTA、列表和受控组件；未为任意业务自动生成专属 reducer |
| 任意代码执行 | 未完成 | 不运行模型生成代码，没有安装依赖、编译和沙箱执行链路 |
| 真实 Publish / 连接器 / 后端 | 未完成 | Publish 与 GitHub connector 为产品交互模拟，无 OAuth 和业务数据库 |

### 评审要求对照

| 要求 | 结论 |
| --- | --- |
| 真实交互 | 计算器、贪吃蛇、通用 Viewer、Design 和完整预览均有可见状态变化，不是截图或静态页面 |
| 数据持久化 | 工作区、对话、产物、主题、设备和预览操作写入 `localStorage`，刷新后恢复 |
| 基本使用流程 | 新建项目、输入需求、审批计划、执行、验收、继续修改可在同一会话完成 |
| 至少一个延展能力 | 已提供 Design、Library、Theme、附件、多项目和工作区导出 |
| 可测试在线链接 | Cloudflare 链接无需登录即可访问，健康检查可确认真实模型状态 |
| 实际可用而非纯 PoC | 核心生成与修改链路具备错误反馈、测试、持久化和公网部署；未完成项已单独标注 |

## 验证结果

| 验证项 | 结果 |
| --- | --- |
| 自动测试 | `npm test`：41 项全部通过，覆盖意图、计划、runtime、schema、计算器、贪吃蛇、增量修改和文档契约 |
| 静态检查 | `npm run check` 通过；服务端、Worker、前端和交互 runtime 均完成语法检查 |
| Diff 检查 | `git diff --check` 通过，无空白错误 |
| Cloudflare 构建 | `wrangler deploy --dry-run` 通过 |
| 真实模型计算器 | DeepSeek 生成计划并构建完成；浏览器点击验证 `12 + 7 = 19`，历史记录正确 |
| 真实模型增量修改 | 同会话识别为 `modify_app`；批准后保持 calculator；`5 % = 0.05`、正负号得到 `-0.05` |
| 真实模型贪吃蛇 | DeepSeek 生成 game 计划并构建完成；浏览器点击开始、方向、暂停、重置，蛇头位置发生变化 |
| 公网可用性 | 首页、`/api/health` 和 GitHub 仓库均以未登录方式检查 |

## 完成边界与已知问题

- “可生成应用”当前分为两层：计算器和贪吃蛇拥有专属行为运行时；其他领域生成的是可操作 UI schema，不等于自动生成任意业务后端。
- 为避免在浏览器执行不可信代码，本版本没有代码沙箱。因此 Code 面板用于检查结构化生成结果，不会安装依赖或运行模型输出的任意 JavaScript。
- 工作区数据保存在当前浏览器 `localStorage`，没有账号同步、协作、版本历史和云端恢复。
- Publish 只模拟发布状态；完整预览复用当前同源工作区，不会为每个生成应用创建独立公网 URL。
- GitHub connector 是演示上下文，不会读取私人仓库；真实连接器需要 OAuth、权限最小化和审计。
- 真实模型仍可能受上游延迟或配额影响；失败时保留本地确定性计划，线上每 IP 每分钟最多 20 次 Agent API 请求。

## 后续扩展优先级

| 优先级 | 方向 | 判断 |
| --- | --- | --- |
| P0 | 隔离代码沙箱 + 编译测试修复循环 | 这是从“受控领域应用”扩展到“任意应用生成”的关键能力，必须先解决文件、网络、进程、资源和凭据隔离 |
| P1 | 独立发布、版本化和回滚 | 沙箱产物可信后，再为每个应用生成稳定 URL，支持部署记录和回滚 |
| P1 | 真实 GitHub / Figma / 数据库连接器 | 能显著扩大输入与业务能力，但需 OAuth、授权、审计和多租户数据隔离 |
| P2 | Diff、Replay 与评测集 | 对比每轮 intent、plan、tool、artifact 和验证结果，提升可解释性与回归质量 |
| P2 | 账号、配额和团队协作 | 当生成应用需要跨设备与多人使用时再投入，当前不阻断核心评审流程 |
